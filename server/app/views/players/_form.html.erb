<%= form_with(model: player, local: true) do |form| %>
  <% if player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :power %>
    <%= form.text_field :power, id: :player_power %>
  </div>

  <div class="field">
    <%= form.label :speed %>
    <%= form.text_field :speed, id: :player_speed %>
  </div>

  <div class="field">
    <%= form.label :hp %>
    <%= form.text_field :hp, id: :player_hp %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
